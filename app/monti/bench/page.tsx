import BenchClient from '@/components/monti/BenchClient';

/**
 * Free Monti site-builder bench (no LiveKit / xAI).
 * Hidden: inherits app/monti/layout.tsx robots noindex/nofollow.
 * Not in sitemap.ts.
 */
export default function MontiBenchPage() {
  return <BenchClient />;
}
